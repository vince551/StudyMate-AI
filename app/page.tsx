"use client";
import { useEffect, useMemo, useState } from "react";

type Score = { subject: string; score: number; date: string };
type Session = { subject: string; minutes: number; date: string };
type Entry = { text: string; date: string };

const classOptions = [
  { group: "Primary School", items: ["Grade 1","Grade 2","Grade 3","Grade 4","Grade 5","Grade 6"] },
  { group: "Junior Secondary", items: ["Grade 7","Grade 8","Grade 9"] },
  { group: "Senior Secondary", items: ["Grade 10","Grade 11","Grade 12"] },
  { group: "College / TVET", items: ["Certificate Year 1","Certificate Year 2","Diploma Year 1","Diploma Year 2","Diploma Year 3"] },
  { group: "University", items: ["Year 1","Year 2","Year 3","Year 4","Year 5","Year 6"] },
  { group: "Postgraduate", items: ["Masters Year 1","Masters Year 2","PhD / Doctoral"] },
];
const defaultSubjects = ["Mathematics","English","Kiswahili","Chemistry","Physics","Biology","CRE","Drawing & Design"];
const defaultScores: Score[] = [62,70,67,76,81,78].map((score,i)=>({subject:defaultSubjects[i%4],score,date:`2026-0${i+2}-15`}));
const seedSessions: Session[] = [42,65,48,78,56,88,68].map((minutes,i)=>({subject:defaultSubjects[i%4],minutes,date:`2026-08-${18+i}`}));
const days = ["Mon","Tue","Wed","Thu","Fri","Sat","Sun"];

export default function Home(){
 const [tab,setTab]=useState("dashboard");
 const [profile,setProfile]=useState({name:"",school:"",level:"Grade 10",goal:""});
 const [subjects,setSubjects]=useState<string[]>([]);
 const [newSubject,setNewSubject]=useState("");
 const [activeSubject,setActiveSubject]=useState("");
 const [scores,setScores]=useState<Score[]>([]);
 const [scoreSubject,setScoreSubject]=useState("");
 const [score,setScore]=useState("");
 const [scoreDate,setScoreDate]=useState("");
 const [sessions,setSessions]=useState<Session[]>([]);
 const [sessionSubject,setSessionSubject]=useState("");
 const [minutes,setMinutes]=useState("");
 const [diary,setDiary]=useState("");
 const [entries,setEntries]=useState<Entry[]>([]);
 const [loaded,setLoaded]=useState(false);
 const [plan,setPlan]=useState<Record<string,string[]>>({});

 useEffect(()=>{try{const raw=localStorage.getItem("studymate-data");if(raw){const d=JSON.parse(raw);setProfile(d.profile||profile);setSubjects(d.subjects||[]);setScores(d.scores||[]);setSessions(d.sessions||[]);setEntries(d.entries||[]);setPlan(d.plan||{});setActiveSubject(d.subjects?.[0]||"");setScoreSubject(d.subjects?.[0]||"");setSessionSubject(d.subjects?.[0]||"");}}catch{}setLoaded(true)},[]);
 useEffect(()=>{if(loaded)localStorage.setItem("studymate-data",JSON.stringify({profile,subjects,scores,sessions,entries,plan}))},[loaded,profile,subjects,scores,sessions,entries,plan]);
 const allScores=scores; const average=allScores.length?Math.round(allScores.reduce((a,b)=>a+b.score,0)/allScores.length):0;
 const totalMinutes=sessions.reduce((a,b)=>a+b.minutes,0); const recentSessions=sessions.slice(-7);
 const addSubject=()=>{const s=newSubject.trim();if(s&&!subjects.includes(s)){const next=[...subjects,s];setSubjects(next);setActiveSubject(s);setScoreSubject(s);setSessionSubject(s);setNewSubject("")}};
 const addScore=()=>{const n=Number(score);if(scoreSubject&&n>=0&&n<=100){setScores([...scores,{subject:scoreSubject,score:n,date:scoreDate||new Date().toISOString().slice(0,10)}]);setScore("");setScoreDate("")}};
 const addSession=()=>{const n=Number(minutes);if(sessionSubject&&n>0){setSessions([...sessions,{subject:sessionSubject,minutes:n,date:new Date().toISOString().slice(0,10)}]);setMinutes("")}};
 const saveDiary=()=>{if(diary.trim()){setEntries([{text:diary.trim(),date:new Date().toISOString().slice(0,10)},...entries]);setDiary("")}};
 const addPlan=(day:string)=>{if(!activeSubject)return;setPlan({...plan,[day:[...(plan[day]||[]),activeSubject]]})};
 return <main className="min-h-screen bg-[#f7f9fc] text-slate-800">
  <header className="sticky top-0 z-30 border-b border-slate-200 bg-white/95 backdrop-blur"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4 sm:px-6"><button onClick={()=>setTab("dashboard")} className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-blue-600 font-bold text-white">S</span><span className="font-bold">StudyMate <span className="font-normal text-blue-600">AI</span></span></button><nav className="hidden gap-1 text-sm lg:flex">{[["dashboard","Dashboard"],["planner","Planner"],["habits","Study habits"],["scores","Exam scores"],["diary","Study diary"],["profile","Profile"]].map(([id,label])=><button key={id} onClick={()=>setTab(id)} className={`rounded-lg px-3 py-2 ${tab===id?"bg-blue-50 font-medium text-blue-700":"text-slate-500 hover:bg-slate-50"}`}>{label}</button>)}</nav><button onClick={()=>setTab("profile")} className="grid h-9 w-9 place-items-center rounded-full border border-slate-200 bg-slate-50 text-sm font-semibold text-slate-600">{profile.name?.[0]?.toUpperCase()||"S"}</button></div></header>
  <div className="mx-auto max-w-7xl px-4 py-6 sm:px-6">
   {tab==="dashboard"&&<><section className="grid gap-6 lg:grid-cols-[1fr_320px]"><div><div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-sm font-semibold text-blue-600">{profile.name?`Welcome back, ${profile.name}`:"Welcome to your education workspace"}</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-slate-900">Plan. Study. Track. Improve.</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Everything shown here comes from your own student profile, timetable, study sessions, diary and exam results.</p><div className="mt-6 grid gap-3 sm:grid-cols-3"><button onClick={()=>setTab("planner")} className="rounded-xl bg-blue-600 p-4 text-left text-white"><b className="block text-sm">Build timetable</b><span className="mt-1 block text-xs text-blue-100">Use your subjects</span></button><button onClick={()=>setTab("habits")} className="rounded-xl border border-slate-200 p-4 text-left"><b className="block text-sm">Log study time</b><span className="mt-1 block text-xs text-slate-400">Track real sessions</span></button><button onClick={()=>setTab("scores")} className="rounded-xl border border-slate-200 p-4 text-left"><b className="block text-sm">Record scores</b><span className="mt-1 block text-xs text-slate-400">Build your own graph</span></button></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Metric title="Study time" value={`${Math.floor(totalMinutes/60)}h ${totalMinutes%60}m`} sub="Your recorded sessions"/><Metric title="Exam average" value={average?`${average}%`:"—"} sub={allScores.length?`${allScores.length} results recorded`:"Add your first result"}/><Metric title="Subjects" value={String(subjects.length)} sub="In your profile"/><Metric title="Diary entries" value={String(entries.length)} sub="Your learning journal"/></div></div><aside className="rounded-2xl border border-slate-200 bg-white p-5"><h2 className="font-semibold">Today&apos;s plan</h2>{days.slice(0,4).map(day=><div key={day} className="mt-3 rounded-xl bg-slate-50 p-3"><div className="flex justify-between text-xs"><b>{day}</b><button onClick={()=>addPlan(day)} className="text-blue-600">+ add</button></div>{(plan[day]||[]).map((s,i)=><p key={i} className="mt-2 text-sm text-blue-700">{s}</p>)}</div>)}<button onClick={()=>setTab("planner")} className="mt-4 w-full rounded-lg border border-slate-200 py-2 text-xs">Open full timetable</button></aside></section><section className="mt-6 rounded-2xl border border-slate-200 bg-white p-6"><div className="flex items-center justify-between"><div><h2 className="font-bold text-slate-900">My subjects</h2><p className="mt-1 text-xs text-slate-400">Add the subjects you actually study.</p></div><button onClick={()=>setTab("profile")} className="text-xs font-medium text-blue-600">Manage →</button></div><div className="mt-4 flex flex-wrap gap-2">{subjects.length?subjects.map(s=><button key={s} onClick={()=>setActiveSubject(s)} className={`rounded-lg px-3 py-2 text-xs ${activeSubject===s?"bg-blue-600 text-white":"bg-slate-50 text-slate-600"}`}>{s}</button>):<p className="text-sm text-slate-400">No subjects yet. Open your profile to add them.</p>}</div></section></>}
   {tab==="planner"&&<Page title="My study planner" desc="Create your own timetable. Nothing is pre-filled — your choices become your plan."><div className="grid gap-6 lg:grid-cols-[1fr_300px]"><div className="rounded-2xl border border-slate-200 bg-white p-5"><h2 className="font-semibold">Weekly timetable</h2>{days.map(day=><div key={day} className="mt-3 rounded-xl border border-slate-100 p-3"><div className="flex items-center justify-between"><b className="text-xs">{day}</b><button onClick={()=>addPlan(day)} className="rounded-lg bg-blue-50 px-2 py-1 text-xs text-blue-700">+ Add selected subject</button></div><div className="mt-2 flex flex-wrap gap-2">{(plan[day]||[]).map((s,i)=><span key={i} className="rounded-lg bg-blue-50 px-3 py-2 text-xs text-blue-700">{s}</span>)}{!(plan[day]||[]).length&&<span className="text-xs text-slate-400">No study session planned.</span>}</div></div>)}</div><div className="rounded-2xl border border-slate-200 bg-white p-5"><h2 className="font-semibold">Plan a session</h2><p className="mt-2 text-xs text-slate-400">Choose one of your subjects, then add it to any day.</p><select value={activeSubject} onChange={e=>setActiveSubject(e.target.value)} className="mt-4 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm"><option value="">Select subject</option>{subjects.map(s=><option key={s}>{s}</option>)}</select><label className="mt-4 block text-xs text-slate-500">Daily target</label><select className="mt-1 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm"><option>1 hour</option><option>2 hours</option><option>3 hours</option><option>4+ hours</option></select><label className="mt-4 block text-xs text-slate-500">Exam date</label><input type="date" className="mt-1 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm"/></div></div></Page>}
   {tab==="habits"&&<Page title="Study habits" desc="Only logged study sessions are used in your charts. Record what you actually studied."><div className="grid gap-6 lg:grid-cols-[1fr_300px]"><div className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="font-semibold">Your study time</h2><div className="mt-8 flex h-64 items-end gap-3">{days.map((day,i)=>{const d=new Date();d.setDate(d.getDate()-(6-i));const key=d.toISOString().slice(0,10);const val=sessions.filter(s=>s.date===key).reduce((a,b)=>a+b.minutes,0);return <div key={day} className="flex flex-1 flex-col items-center gap-2"><span className="text-[10px] text-slate-400">{val||0}m</span><div className="w-full rounded-t-lg bg-blue-500" style={{height:`${Math.max(val,2)/2}%`}}/><span className="text-[10px] text-slate-400">{day}</span></div>})}</div></div><div className="rounded-2xl border border-slate-200 bg-white p-5"><h2 className="font-semibold">Log a study session</h2><select value={sessionSubject} onChange={e=>setSessionSubject(e.target.value)} className="mt-4 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm"><option value="">Select subject</option>{subjects.map(s=><option key={s}>{s}</option>)}</select><input value={minutes} onChange={e=>setMinutes(e.target.value)} type="number" placeholder="Minutes studied" className="mt-2 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm"/><button onClick={addSession} className="mt-2 w-full rounded-lg bg-blue-600 py-2.5 text-xs font-semibold text-white">Log session</button><div className="mt-5 border-t pt-4"><p className="text-xs text-slate-400">Subject distribution</p>{subjects.map(s=>{const m=sessions.filter(x=>x.subject===s).reduce((a,b)=>a+b.minutes,0);return m?<div key={s} className="mt-2 flex justify-between text-xs"><span>{s}</span><b>{m}m</b></div>:null})}</div></div></div></Page>}
   {tab==="scores"&&<Page title="Exam score tracker" desc="Enter your real marks. StudyMate automatically plots your academic performance."><div className="grid gap-6 lg:grid-cols-[1fr_300px]"><div className="rounded-2xl border border-slate-200 bg-white p-6"><div className="flex justify-between"><h2 className="font-semibold">Your performance trend</h2><span className="rounded-full bg-blue-50 px-3 py-1 text-xs text-blue-700">Average {average||"—"}{average?"%":""}</span></div>{scores.length?<div className="mt-8 flex h-64 items-end gap-2 border-b border-slate-100">{scores.map((x,i)=><div key={i} className="group relative flex h-full flex-1 items-end"><div className="w-full rounded-t-md bg-blue-500" style={{height:`${x.score}%`}}/><span className="absolute -top-5 left-1/2 -translate-x-1/2 text-[10px] text-slate-400 opacity-0 group-hover:opacity-100">{x.score}%</span></div>)}</div>:<div className="mt-8 grid h-64 place-items-center rounded-xl bg-slate-50 text-sm text-slate-400">Your graph will appear here after you enter exam scores.</div>}<div className="mt-4 space-y-2">{scores.slice().reverse().map((x,i)=><div key={i} className="flex justify-between rounded-lg bg-slate-50 p-3 text-xs"><span>{x.subject} · {x.date}</span><b>{x.score}%</b></div>)}</div></div><div className="rounded-2xl border border-slate-200 bg-white p-5"><h2 className="font-semibold">Add exam result</h2><select value={scoreSubject} onChange={e=>setScoreSubject(e.target.value)} className="mt-4 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm"><option value="">Select subject</option>{subjects.map(s=><option key={s}>{s}</option>)}</select><input value={score} onChange={e=>setScore(e.target.value)} type="number" min="0" max="100" placeholder="Score / 100" className="mt-2 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm"/><input value={scoreDate} onChange={e=>setScoreDate(e.target.value)} type="date" className="mt-2 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm"/><button onClick={addScore} className="mt-2 w-full rounded-lg bg-blue-600 py-2.5 text-xs font-semibold text-white">Add to graph</button></div></div></Page>}
   {tab==="diary"&&<Page title="Study diary" desc="Your personal record of the education journey."><div className="grid gap-6 lg:grid-cols-[1fr_300px]"><div className="rounded-2xl border border-slate-200 bg-white p-5"><textarea value={diary} onChange={e=>setDiary(e.target.value)} rows={8} placeholder="What did you study today? What did you understand? What was difficult?" className="w-full resize-none rounded-xl border border-slate-200 p-4 text-sm outline-none focus:border-blue-400"/><button onClick={saveDiary} className="mt-3 rounded-lg bg-blue-600 px-4 py-2.5 text-xs font-semibold text-white">Save entry</button><div className="mt-6 space-y-3">{entries.map((e,i)=><article key={i} className="rounded-xl bg-slate-50 p-4"><p className="text-[10px] uppercase text-slate-400">{e.date}</p><p className="mt-2 text-sm leading-6 text-slate-600">{e.text}</p></article>)}</div></div><div className="rounded-2xl border border-slate-200 bg-white p-5"><h2 className="font-semibold">Reflection prompts</h2>{["What did I learn?","What needs more revision?","What worked today?","What is tomorrow's target?"] .map(x=><div key={x} className="mt-3 rounded-lg bg-slate-50 p-3 text-xs text-slate-600">{x}</div>)}</div></div></Page>}
   {tab==="profile"&&<Page title="My student profile" desc="Set your education level and subjects. Your dashboard will use your information everywhere."><div className="grid gap-6 md:grid-cols-2"><div className="rounded-2xl border border-slate-200 bg-white p-6"><div className="flex items-center gap-4"><div className="grid h-16 w-16 place-items-center rounded-full bg-blue-100 text-xl font-bold text-blue-700">{profile.name?.[0]?.toUpperCase()||"S"}</div><div><h2 className="font-bold">Student details</h2><p className="text-xs text-slate-400">This information personalizes your workspace.</p></div></div><input value={profile.name} onChange={e=>setProfile({...profile,name:e.target.value})} placeholder="Full name" className="mt-6 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm"/><input value={profile.school} onChange={e=>setProfile({...profile,school:e.target.value})} placeholder="School / college / university" className="mt-2 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm"/><select value={profile.level} onChange={e=>setProfile({...profile,level:e.target.value})} className="mt-2 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm">{classOptions.map(g=><optgroup key={g.group} label={g.group}>{g.items.map(x=><option key={x}>{x}</option>)}</optgroup>)}</select><input value={profile.goal} onChange={e=>setProfile({...profile,goal:e.target.value})} placeholder="Main education goal" className="mt-2 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm"/></div><div className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="font-semibold">My subjects</h2><p className="mt-1 text-xs text-slate-400">Add only the subjects you currently take.</p><div className="mt-4 flex flex-wrap gap-2">{subjects.map(s=><button key={s} onClick={()=>setActiveSubject(s)} className={`rounded-lg px-3 py-2 text-xs ${activeSubject===s?"bg-blue-600 text-white":"bg-blue-50 text-blue-700"}`}>{s}</button>)}</div><div className="mt-5 flex gap-2"><input value={newSubject} onChange={e=>setNewSubject(e.target.value)} placeholder="Add subject" className="min-w-0 flex-1 rounded-lg border border-slate-200 px-3 py-2 text-sm"/><button onClick={addSubject} className="rounded-lg bg-blue-600 px-3 py-2 text-xs font-semibold text-white">Add</button></div></div></div></Page>}
  </div>
 </main>
}
function Metric({title,value,sub}:{title:string,value:string,sub:string}){return <div className="rounded-2xl border border-slate-200 bg-white p-5"><p className="text-xs text-slate-400">{title}</p><p className="mt-1 text-2xl font-bold text-slate-900">{value}</p><p className="mt-1 text-xs text-slate-400">{sub}</p></div>}
function Page({title,desc,children}:{title:string,desc:string,children:React.ReactNode}){return <section><div className="mb-6"><h1 className="text-3xl font-bold tracking-tight text-slate-900">{title}</h1><p className="mt-2 text-sm text-slate-500">{desc}</p></div>{children}</section>}
